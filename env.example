# Configuration du serveur
PORT=10000
NODE_ENV=development

# Configuration CORS
FRONTEND_URL=https://ton-frontend.vercel.app

# Configuration JWT (optionnel pour l'instant)
JWT_SECRET=votre-secret-jwt-super-securise-pour-bms

# Configuration de la base de données (à configurer plus tard)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bms_db
DB_USER=bms_user
DB_PASSWORD=bms_password

# Configuration Render (sera automatiquement définie par Render)
# PORT sera défini automatiquement par Render
# NODE_ENV sera défini automatiquement par Render

# Configuration des logs
LOG_LEVEL=info 